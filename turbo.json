{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "dev": {
      "dependsOn": ["generate-types"],
      "persistent": true,
      "cache": false
    },
    "clean": {
      "cache": false
    },
    "generate-types": {
      "outputs": ["payload-types.ts"]
    }
  }
}
