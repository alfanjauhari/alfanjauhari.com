import { styled } from '@/theme';

export const StyledPre = styled('pre', {
  my: '$5',
  backgroundColor: '$gray1',
  overflowX: 'auto',
  fontSize: '14px',
  lineHeight: 1.7,
  p: '$4',
  borderRadius: '5px',
});
